---
title: Litex timer
id: litex,timer
version: 0.1

description: >
    This binding gives a base representation of LiteX timer

properties:
    compatible:
      type: string
      category: required
      description: compatible strings
      constraint: "litex,timer0"

    reg:
      type: array
      description: mmio register space
      generation: define
      category: required

    interrupts:
      type: array
      category: required
      description: required interrupts
      generation: define
...
